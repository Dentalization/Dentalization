// Test file to verify configuration
const message: string = 'Hello Dentalization!';

function greet(name: string): string {
  return `Hello, ${name}!`;
}

console.log(greet('World'));

export { greet };
